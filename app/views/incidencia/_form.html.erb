<%= form_for(@incidencium) do |f| %>
  <% if @incidencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@incidencium.errors.count, "error") %> prohibited this incidencium from being saved:</h2>

      <ul>
      <% @incidencium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
