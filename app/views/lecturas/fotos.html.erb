<% @lectura.historico_de_fotos_del_medidor.each do |foto| %>
    
 <%= link_to url_for(:controller => "main", :action => "foto", :id => foto.id,  only_path: false), :target => "_blank"  do %>
 	<%= image_tag url_for(:controller => "main", :action => "foto", :id => foto.id,  only_path: false), size: "150" %>
 <% end %>

<% end%>

